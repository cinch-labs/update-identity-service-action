export enum Input {
  UPDATE_TYPE = 'update-type',
  ACCESS_KEY = 'access-key',
  SUBDOMAIN_INFIX = 'subdomain-infix',
  AUTH_AUTHORITY = 'auth-authority',
}

export enum UpdateType {
  ADD = 'add',
  REMOVE = 'remove',
}
